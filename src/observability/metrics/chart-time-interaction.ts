/**
 * @deprecated Import from `@/components/raw-chart/time-interaction` — kept as a shim
 * for existing observability imports/tests.
 */
export {
  GRAFANA_MIN_PAN_DIST_PX,
  GRAFANA_MIN_ZOOM_DIST_PX,
  computePanRange,
  computeZoomOutRange,
  isModifierBlockedZoom,
  toUnixTimeRangeSeconds,
  attachTimeInteraction,
  type ChartTimeRangeMs,
} from '@/components/raw-chart/time-interaction'
